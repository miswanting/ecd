import{Q as e,X as t,Y as n,ct as r,ft as i,t as a}from"./chunks/framework.CaFWb3SA.js";var o=JSON.parse(`{"title":"错误索引表","description":"","frontmatter":{},"headers":[],"relativePath":"reference/Error_Index.md","filePath":"reference/Error_Index.md"}`),s={name:`reference/Error_Index.md`};function c(a,o,s,c,l,u){let d=i(`ErrorIndex`);return r(),n(`div`,null,[o[0]||=t(`<h1 id="错误索引表" tabindex="-1">错误索引表 <a class="header-anchor" href="#错误索引表" aria-label="Permalink to “错误索引表”">​</a></h1><p>本页汇总 Emuera 常见的错误与警告信息。前几节是按含义整理的<strong>常见问题</strong>；页面末尾还嵌入了从引擎源码提取的<strong>完整错误信息索引</strong>，可以按日文原文搜索。</p><blockquote><p>报错时，除了画面上的信息，根目录下的 <code>emuera.log</code> 里还有更详细的记录（文件名、行号、函数名等）。排错方法的教程版见<a href="./../guide/tutorials/Error.html">错误与异常</a>。</p></blockquote><h2 id="运行错误" tabindex="-1">运行错误 <a class="header-anchor" href="#运行错误" aria-label="Permalink to “运行错误”">​</a></h2><p>在游戏运行过程中出现。</p><table tabindex="0"><thead><tr><th>错误信息（原文）</th><th>含义</th><th>常见原因与对策</th></tr></thead><tbody><tr><td><code>関数&quot;@XXX&quot;が見つかりません</code></td><td>找不到函数 <code>@XXX</code></td><td>函数名拼错、忘了定义，或定义在未被读取的文件里</td></tr><tr><td><code>予期しないスクリプト終端です</code></td><td>预料之外的脚本结尾</td><td>函数或复合语句没有写完，检查出错位置附近</td></tr><tr><td><code>配列の範囲外です</code></td><td>访问了数组范围之外的位置</td><td>数组从 0 开始；检查下标是否越界或为负</td></tr><tr><td><code>...はキャラ登録番号の範囲外です</code></td><td>角色编号超出范围</td><td>检查 <code>ADDCHARA</code> / 角色变量使用的编号</td></tr><tr><td><code>代入文の左辺に変更できない変数を指定することはできません</code></td><td>不能给不可修改的变量赋值</td><td>不要给常量、伪变量或只读变量赋值</td></tr><tr><td><code>&#39;=&#39;の前後で型が一致しません</code></td><td>赋值号两边类型不同</td><td>字符串变量要用 <code>#DIMS</code> 定义，值要加引号</td></tr><tr><td><code>ASSERT文の引数が0です</code></td><td><code>ASSERT</code> 断言失败</td><td>前提条件不成立；检查触发该断言的数据</td></tr><tr><td><code>...は数値として解釈できません</code></td><td>无法作为数值解释</td><td>检查该处是否应当传入字符串</td></tr><tr><td><code>...が64ビット符号付き整数の範囲外です</code></td><td>数值超出 64 位整数范围</td><td>检查计算是否溢出，或输入的数值过大</td></tr></tbody></table><h2 id="代码错误-语法与结构" tabindex="-1">代码错误（语法与结构） <a class="header-anchor" href="#代码错误-语法与结构" aria-label="Permalink to “代码错误（语法与结构）”">​</a></h2><p>在加载脚本、解析语法时出现。</p><table tabindex="0"><thead><tr><th>错误信息（原文）</th><th>含义</th><th>常见原因与对策</th></tr></thead><tbody><tr><td><code>IFに対応するENDIFが設定されていない</code></td><td>缺少与 <code>IF</code> 配对的 <code>ENDIF</code></td><td>补齐 <code>ENDIF</code>；用编辑器的折叠功能检查嵌套</td></tr><tr><td><code>SELECTCASEに対応するENDSELECTが設定されていない</code></td><td><code>SELECTCASE</code> 没有对应的 <code>ENDSELECT</code></td><td>补齐 <code>ENDSELECT</code></td></tr><tr><td><code>IF～ENDIFの外で...</code></td><td>在 <code>IF～ENDIF</code> 之外使用了只能在内部使用的命令</td><td>把该命令移进 <code>IF～ENDIF</code> 之内</td></tr><tr><td><code>REPEAT, FOR, WHILE, DOの中以外で...</code></td><td>在循环之外使用了 <code>BREAK</code> / <code>CONTINUE</code> 等</td><td>只在循环内使用循环控制</td></tr><tr><td><code>ELSE文より後で...</code> / <code>CASEELSE文より後で...</code></td><td>在 <code>ELSE</code> / <code>CASEELSE</code> 之后使用了不该用的命令</td><td>调整顺序</td></tr><tr><td><code>SIF文の次の行がありません</code></td><td><code>SIF</code> 后面没有下一行</td><td>在 <code>SIF</code> 下一行写上要执行的语句</td></tr><tr><td><code>SIF文の次の行をラベル行にすることはできません</code></td><td><code>SIF</code> 的下一行是标签</td><td>调整代码顺序</td></tr><tr><td><code>SIF文の次の行が空行またはコメント行です(eramaker:SIF文は意味を失います)</code></td><td><code>SIF</code> 的下一行是空行 / 注释</td><td>删掉中间的空行、注释</td></tr><tr><td><code>REPEAT文が入れ子にされています（無限ループの恐れがあります）</code></td><td><code>REPEAT</code> 嵌套异常</td><td>检查 <code>REPEAT</code> / <code>REND</code> 是否配对</td></tr><tr><td><code>&#39;(&#39;に対応する&#39;)&#39;が見つかりません</code></td><td>缺少配对的 <code>)</code></td><td>检查括号是否成对</td></tr><tr><td><code>&#39;[&#39;に対応する&#39;]&#39;が見つかりません</code></td><td>缺少配对的 <code>]</code></td><td>检查方括号是否成对</td></tr><tr><td><code>&#39;{\\&#39;が使われましたが対応する\\&#39;}\\&#39;が見つかりません</code></td><td>行连接 <code>{</code> 没有闭合</td><td>补齐 <code>}</code></td></tr><tr><td><code>&#39;%&#39;が使われましたが対応する&#39;%&#39;が見つかりません</code></td><td>FORM 的 <code>%</code> 没有闭合</td><td>补齐成对的 <code>%</code></td></tr><tr><td><code>&#39;?と&#39;#&#39;の数が正しく対応していません</code></td><td>三元的 <code>?</code> 与 <code>#</code> 数量不匹配</td><td>检查三元表达式的写法</td></tr><tr><td><code>$で始まるラベルに引数が設定されています</code></td><td>标签后面写了参数</td><td>标签不能带参数</td></tr><tr><td><code>@の使い方が不正です</code></td><td><code>@</code> 用法不正确</td><td>检查函数定义行</td></tr><tr><td><code>DATALISTが閉じられていません</code></td><td><code>DATALIST</code> 没有闭合</td><td>补齐 <code>ENDLIST</code></td></tr></tbody></table><h2 id="警告" tabindex="-1">警告 <a class="header-anchor" href="#警告" aria-label="Permalink to “警告”">​</a></h2><p>警告不会中断运行，但往往预示着潜在问题。</p><table tabindex="0"><thead><tr><th>警告信息（原文）</th><th>含义</th><th>建议</th></tr></thead><tbody><tr><td><code>#FUNCTIONで始まる関数の戻り値に文字列型が指定されました</code></td><td><code>#FUNCTION</code> 函数返回了字符串</td><td>数值函数应返回数值</td></tr><tr><td><code>#FUCNTIONSで始まる関数の戻り値に数値型が指定されました</code></td><td><code>#FUNCTIONS</code> 函数返回了数值</td><td>字符串函数应返回字符串</td></tr><tr><td><code>#PRIと#LATERが重複して使われています(この関数は2度呼ばれます)</code></td><td>同时指定 <code>#PRI</code> 与 <code>#LATER</code></td><td>该函数会被调用两次，确认是否本意</td></tr><tr><td><code>#ONLYが指定されたイベント関数では#PRIは機能しません</code></td><td><code>#ONLY</code> 使 <code>#PRI</code> 失效</td><td>不要同时使用这两个属性</td></tr><tr><td><code>#ONLYが指定されたイベント関数では#LATERは機能しません</code></td><td><code>#ONLY</code> 使 <code>#LATER</code> 失效</td><td>同上</td></tr><tr><td><code>#ONLYが指定されたイベント関数では#SINGLEは機能しません</code></td><td><code>#ONLY</code> 使 <code>#SINGLE</code> 失效</td><td>同上</td></tr><tr><td><code>#PRIが重複して使われています</code></td><td><code>#PRI</code> 重复</td><td>每个函数只写一处</td></tr><tr><td><code>この関数にはすでに#LOCALSIZEが定義されています。（以前の定義は無視されます）</code></td><td>重复定义 <code>#LOCALSIZE</code></td><td>每个函数只保留一处</td></tr><tr><td><code>（構文上の注意）比較演算子が連続しています。</code></td><td>比较运算符连续出现</td><td>检查表达式是否写错</td></tr><tr><td><code>[]の使い方が不正です</code></td><td><code>[]</code> 用法不正确</td><td>检查特殊区块的写法</td></tr><tr><td><code>[SKIPSTART]が重複して使用されています</code></td><td><code>[SKIPSTART]</code> 重复</td><td>检查区块是否嵌套错误</td></tr><tr><td><code>[SKIPSTART]と対応しない[SKIPEND]です</code></td><td><code>[SKIPEND]</code> 没有对应的 <code>[SKIPSTART]</code></td><td>补齐配对</td></tr></tbody></table><h2 id="完整错误信息索引" tabindex="-1">完整错误信息索引 <a class="header-anchor" href="#完整错误信息索引" aria-label="Permalink to “完整错误信息索引”">​</a></h2><p>下面是从 Emuera 参考工程源码（<code>ParserMediator.Warn</code> / <code>CodeEE</code> / <code>ExeEE</code> / <code>PrintError</code>）中提取的<strong>全部错误与警告文案</strong>，共 500 余条。由于数量庞大，这里保持<strong>日文原文</strong>（你实际看到的就是这些），并标注级别与来源文件，方便搜索定位。</p><ul><li><strong>级别</strong>：<code>警告</code>（不中断）、<code>错误</code>（中断当前脚本）、<code>致命</code>（无法恢复）。</li><li>想了解某条信息的含义与处理，可先在上面的常见表中查找；更深入的可对照<a href="./../development/Emuera.html">开发</a>中的源码导读。</li></ul>`,15),e(d),o[1]||=t(`<h2 id="相关链接" tabindex="-1">相关链接 <a class="header-anchor" href="#相关链接" aria-label="Permalink to “相关链接”">​</a></h2><ul><li><a href="./../guide/tutorials/Error.html">错误与异常</a>（教程）</li><li><a href="./../translation/Debug_Mode.html">调试模式</a></li><li><a href="./../translation/Function_and_Preprocessor.html">函数与预处理指令</a></li><li><a href="./ERB_Variables.html">ERB 的变量</a></li></ul>`,2)])}var l=a(s,[[`render`,c]]);export{o as __pageData,l as default};