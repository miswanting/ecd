import{o as e,s as t,t as n,u as r}from"./app--qkcJxTK.js";var i=JSON.parse(`{"path":"/translation/Replace_CSV-df32.html","title":"关于 _replace.csv（风飏@df32翻译版）","lang":"zh-CN","frontmatter":{},"git":{"updatedTime":1789556905000,"contributors":[{"name":"Miswanting","username":"Miswanting","email":"ihex@foxmail.com","commits":1,"url":"https://github.com/Miswanting"}],"changelog":[{"hash":"f4aaaa665dd6d7cbce434ec5e6682b3aee19b711","time":1789556905000,"email":"ihex@foxmail.com","author":"Miswanting","message":"ci: bump toolchain to Node 24 and pnpm 12"}]},"filePathRelative":"translation/Replace_CSV-df32.md"}`),a={name:`Replace_CSV-df32.md`};function o(n,i,a,o,s,c){return r(),e(`div`,null,[...i[0]||=[t(`<h1 id="关于-replace-csv-风飏-df32翻译版" tabindex="-1"><a class="header-anchor" href="#关于-replace-csv-风飏-df32翻译版"><span>关于 _replace.csv（风飏@df32翻译版）</span></a></h1><blockquote><p>翻译自原文档：https://osdn.net/projects/emuera/wiki/replace</p></blockquote><p>译者：<a href="https://github.com/df32" target="_blank" rel="noopener noreferrer">风飏@df32</a></p><p>也可跳转至：<a href="Replace_CSV">原版</a></p><p>CSV文件夹中的<code>_replace.csv</code>中包含了一系列脚本的设置。</p><h2 id="金钱单位" tabindex="-1"><a class="header-anchor" href="#金钱单位"><span>金钱单位</span></a></h2><p><code>お金の単位</code>用于设置<code>PRINT_SHOPITEM</code>命令显示商品价格时的金钱单位。</p><p>默认值是<code>$</code>，也可以指定为多字节字符或多个字符组成的文本。</p><h2 id="单位位置" tabindex="-1"><a class="header-anchor" href="#单位位置"><span>单位位置</span></a></h2><p><code>単位の位置</code>用于设置<code>PRINT_SHOPITEM</code>指令显示商品价格时，金钱单位显示在数字的前后位置。</p><p>有效值为<code>前</code>或<code>後</code>。默认值为<code>後</code>。</p><h2 id="启动时显示文字" tabindex="-1"><a class="header-anchor" href="#启动时显示文字"><span>启动时显示文字</span></a></h2><p><code>起動時簡略表示</code>用于设置当Emuera设置关闭了<code>加载时显示报告</code>时，Emuera启动加载脚本时显示的文字。</p><p>默认为<code>Now Loading...</code>。</p><h2 id="出售物品数" tabindex="-1"><a class="header-anchor" href="#出售物品数"><span>出售物品数</span></a></h2><p><code>販売アイテム数</code>用于设置购买商品过程中，哪些数字表示商品（进而调用<code>@EVENTBUY</code>），哪些数字表示用户选项（进而调用<code>@USERSHOP</code>）。</p><p>在 Eramaker 的正常<code>SHOP</code>处理中，<code>PRINT_SHOPITEM</code>将显示所有<code>ITEMSALES</code>为非零的<code>ITEMNAME</code>(0-999)。 另一方面，只有当输入0-99时，购买过程和<code>@EVENTBUY</code>才会发生，如果输入任何其他数值，<code>@USERSHOP</code>将被调用。</p><p>Emuera本质上复制了这个过程。 改变<code>販売アイテム数</code>将改变购买过程将被调用的物品范围。</p><p>例如，如果你把<code>販売アイテム数</code>设置为<code>1000</code>，那么如果你输入的数值在0到999之间，那么购买就会被处理，但是如果你输入一个负值或者一个大于1000的数值，那么<code>@USERSHOP</code>将被调用。</p><blockquote><p>注意：如果你输入一个介于0和<code>販売アイテム数-1</code>之间的值，无论购买成功与否，<code>@USERSHOP</code>都不会被调用。</p></blockquote><p>例如，如果<code>販売アイテム数</code>是<code>1000</code>，<code>[200]-保存</code>或<code>[300]-加载</code>将被认为是试图购买物品200或300，<code>@USERSHOP</code>将不会被调用。</p><p>如果你想增加或减少<code>PRINT_SHOPITEM</code>中显示的项目数量，请改变<code>VariableSize.csv</code>中<code>ITEMNAME</code>和<code>ITEMALES</code>数组的元素数量。</p><p><code>ITEMNAME</code>和<code>ITEMSALES</code>数组中较小的元素数量将是<code>PRINT_SHOPITEM</code>中显示的项目范围。</p><h2 id="drawline文字" tabindex="-1"><a class="header-anchor" href="#drawline文字"><span>DRAWLINE文字</span></a></h2><p><code>DRAWLINE文字</code>用于设置<code>DRAWLINE</code>指令绘制分割线时使用的字符。</p><p>默认字符是<code>-</code>。</p><h2 id="bar文字" tabindex="-1"><a class="header-anchor" href="#bar文字"><span>BAR文字</span></a></h2><p><code>BAR文字1</code>与<code>BAR文字2</code>用于设置<code>BAR</code>或<code>BARL</code>指令中绘制长条时使用的字符。</p><p>默认分别为<code>*</code>、<code>.</code>。</p><p>在这种情况下显示为<code>[****....]</code>。</p><p>也可以指定为多个字符组成的文本，但可能会导致显示不正常。</p><p>另外，如果指定数字，Emuera会认为[88881111]或类似的数字是一个可点击的按钮，由此出现问题。</p><h2 id="系统菜单" tabindex="-1"><a class="header-anchor" href="#系统菜单"><span>系统菜单</span></a></h2><p><code>システムメニュー0</code>、<code>システムメニュー1</code>用于游戏加载后标题画面显示的两个选项的文字。</p><p>默认值分别为<code>最初からはじめる</code>（<code>新的开始</code>）、<code>ロードしてはじめる</code>（载入存档）。</p><p>默认标题画面的选项显示为</p><div class="language-text line-numbers-mode line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre class="shiki slack-dark vp-code" style="background-color:#222222;color:#E6E6E6;"><code><span class="line"><span class="line"><span>[0] 最初からはじめる</span></span></span>
<span class="line"><span class="line"><span>[1] ロードしてはじめる</span></span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"></div><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>当使用<code>@SYSTEM_TITLE</code>来实现用户自定义的标题画面时，这里设置的文字将不会被使用。 请在<code>@SYSTEM_TITLE</code>内实现选项的显示。</p><h2 id="com-able初始值" tabindex="-1"><a class="header-anchor" href="#com-able初始值"><span>COM_ABLE初始值</span></a></h2><p><code>COM_ABLE初期値</code>用于设置<strong>TRAIN</strong>过程中，没有找到<code>@COM_ABLE{X}</code>时，是否在调教指令菜单上显示该指令。</p><blockquote><p><code>@COM_ABLE{X}</code>函数用于确定一个调教指令在当前状态下对当前调教目标是否可用。 若函数返回<code>1</code>，则表示该调教指令可用，并在调教指令菜单中显示该指令。 若函数返回<code>0</code>，则表示该调教指令不可用，将不会显示该指令。</p></blockquote><p>该设置默认值为1，即当某个调教指令没有<code>@COM_ABLE{X}</code>函数时，该调教指令始终可用。</p><h2 id="污秽初始值" tabindex="-1"><a class="header-anchor" href="#污秽初始值"><span>污秽初始值</span></a></h2><p><code>汚れの初期値</code>用于设置当<code>STAIN</code>初始化时的初始值。</p><p><code>STAIN:1</code>之后的数值可以用<code>/</code>分隔。</p><p>默认为<code>0, 0, 2, 1, 8 </code>。</p><h2 id="超时显示" tabindex="-1"><a class="header-anchor" href="#超时显示"><span>超时显示</span></a></h2><p><code>時間切れ表示</code>用于设置当使用限时输入指令<code>TINPUT</code>输入超时的时候显示的文字。</p><p>默认为<code>時間切れ</code>（超过时间）。</p><h2 id="explv初始值" tabindex="-1"><a class="header-anchor" href="#explv初始值"><span>EXPLV初始值</span></a></h2><p><code>EXPLVの初期値</code>用于设置<code>EXPLV</code>的默认值，即调教经验（<code>EXP</code>）每上升一个等级所需要的数值。</p><p><code>EXPLV:1</code>之后的数值可以用<code>/</code>分隔。</p><p>默认为<code>0, 1, 4, 20, 50, 200</code>。</p><h2 id="palamlv初始值" tabindex="-1"><a class="header-anchor" href="#palamlv初始值"><span>PALAMLV初始值</span></a></h2><p><code>PALAMLVの初期値</code>用于设置<code>PALAMLV</code>的默认值，即调教参数（<code>PALAM</code>）每上升一个等级所需要的数值。</p><p><code>PALAMLV:1</code>之后的数值可以用<code>/</code>分隔。</p><p>默认为<code>0, 100, 500, 3000, 10000, 30000, 60000, 100000, 150000, 250000</code>。</p><h2 id="pband初始值" tabindex="-1"><a class="header-anchor" href="#pband初始值"><span>PBAND初始值</span></a></h2><p><code>PBANDの初期値</code>用于设置<code>PBAND:0</code>的默认值。</p><p>默认为<code>4</code>。</p><h2 id="relation初始值" tabindex="-1"><a class="header-anchor" href="#relation初始值"><span>RELATION初始值</span></a></h2><p><code>RELATIONの初期値</code>用于设置<code>Chara*XX.csv</code>中两个角色之间没有指定相性（<code>RELATION</code>）时，这两个角色之间的<code>RELATION</code>的初始值。</p><p>默认为<code>0</code>。</p>`,63)]])}var s=n(a,[[`render`,o]]);export{i as _pageData,s as default};