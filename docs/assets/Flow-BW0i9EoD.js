import{a as e,d as t,l as n,o as r,s as i,t as a,u as o}from"./app--qkcJxTK.js";var s=JSON.parse(`{"path":"/translation/Flow.html","title":"流程图","lang":"zh-CN","frontmatter":{},"git":{"updatedTime":1789556905000,"contributors":[{"name":"Miswanting","username":"Miswanting","email":"ihex@foxmail.com","commits":1,"url":"https://github.com/Miswanting"}],"changelog":[{"hash":"f4aaaa665dd6d7cbce434ec5e6682b3aee19b711","time":1789556905000,"email":"ihex@foxmail.com","author":"Miswanting","message":"ci: bump toolchain to Node 24 and pnpm 12"}]},"filePathRelative":"translation/Flow.md"}`),c={name:`Flow.md`};function l(a,s,c,l,u,d){let f=t(`FlowChart`);return o(),r(`div`,null,[e(` <script setup>
import FlowTitle from './FlowTitle.vue'
<\/script> `),s[0]||=i(`<h1 id="流程图" tabindex="-1"><a class="header-anchor" href="#流程图"><span>流程图</span></a></h1><blockquote><p>翻译自原文档：<a href="https://osdn.net/projects/emuera/wiki/flow" target="_blank" rel="noopener noreferrer">https://osdn.net/projects/emuera/wiki/flow</a></p></blockquote><p>本章节所涉及的流程图是用 <a href="http://logicnet.dk/DiagramDesigner/" target="_blank" rel="noopener noreferrer">DiagramDesigner</a> 编辑的。流程图源文件文件在<a href="https://osdn.net/projects/emuera/wiki/flow/attach/flow1821.ddd" target="_blank" rel="noopener noreferrer">这里</a>。</p><p>后文除另有说明，否则句子的主语应是<code>Emuera.exe</code>。</p><h2 id="title-标题流程" tabindex="-1"><a class="header-anchor" href="#title-标题流程"><span>Title 标题流程</span></a></h2><p>流程进入方式：</p><ul><li>自动：游戏引擎启动后会读取<code>ERB</code>文件，文件全部读取完成后，自动进入；</li><li>手动：使用<code>Begin Title</code>语句手动进入。</li></ul><p><img src="https://osdn.net/projects/emuera/wiki/flow/attach/title.gif" alt=""></p><p>若<code>@System_Title</code>函数已定义，则调用它；若存在多个定义，则调用第一次读取到的定义。</p><p>如果<code>@System_Title</code>函数在<code>Return</code>之前，没有调用<code>Begin</code>或<code>LoadData</code>命令，则会报错退出。</p><p>若没有定义<code>@System_Title</code>，就会执行后续的默认标题流程。</p><p>默认标题中的文字，如标题、版本、介绍、命令按钮（如<code>[0]从头开始</code>）等，都可以被改变，详见 <a href="Replace_CSV#%E7%B3%BB%E7%BB%9F%E8%8F%9C%E5%8D%950-%E7%B3%BB%E7%BB%9F%E8%8F%9C%E5%8D%951">_replace.csv</a>。</p><p>在这个界面中，若选择<code>[0]从头开始</code>，首先要做的就是初始化数据。如<code>Str</code>和<code>PrintLV</code>的初始值（与<code>ResetData</code>命令同理），<code>AddChara 0</code>等。</p><p>然后自动执行<code>Begin First</code>命令。进入<code>First</code>流程。</p><p>若选择<code>[1]加载存档</code>，若已定义<code>@Title_LoadGame</code>函数，则调用。</p><p>若未定义，则显示默认加载界面。该界面与<code>@LoadGame</code>所调用的界面略有不同。</p><h2 id="first-从头开始流程" tabindex="-1"><a class="header-anchor" href="#first-从头开始流程"><span>First <code>从头开始</code>流程</span></a></h2><p>流程进入方式：</p><ul><li>自动：在标题界面选择了<code>[0]从头开始</code>后，自动进入；</li><li>手动：使用<code>Begin First</code>语句手动进入。</li></ul><blockquote><p>https://osdn.net/projects/emuera/wiki/flow/attach/first.gif</p></blockquote>`,20),n(f,{code:`eJwrLkksKrG1A1NWCm6ZRcUlXKllqXkl8WkgNlCmNKkov7QkMy/VSsHBFSQDVZSXYmsHJKwUXIuK8ou4wCbo2iHpBXLyUrgA8Ncjqw==`,preset:`vue`}),s[1]||=i(`<p>如果<code>@EventFirst</code>函数在<code>Return</code>之前，没有调用<code>Begin</code>命令，则会报错退出。</p><h2 id="shop-商店流程" tabindex="-1"><a class="header-anchor" href="#shop-商店流程"><span>Shop 商店流程</span></a></h2><div class="hint-container warning"><p class="hint-container-title">注意</p><p>此处流程的名称具有迷惑性，虽然名称是商店，但其实并不是指游戏中的物品购买流程，而是游戏的行动主界面，即行动按钮最多的那个界面。一般来说，可以理解为行动按钮分层排列就像商店里的商品一样的那种感觉。</p></div><p>流程进入方式：</p><ul><li>自动：加载存档后自动进入；</li><li>手动：使用<code>Begin Shop</code>语句手动进入。</li></ul><p><img src="https://osdn.net/projects/emuera/wiki/flow/attach/shop.gif" alt=""></p><p>如果该流程是从加载存档处进入，则不进行<code>@EventShop</code>处理。</p><p>在<code>@Show_Shop</code>执行完毕后，等待输入，若输入序号在0~99内，则执行内置购买逻辑，若不在内，则调用<code>@UserShop</code>函数。这个数值范围可被改变，详见 <a href="Replace_CSV">_replace.csv</a>。</p><p>注意：<code>@Print_ItemShop</code>函数显示的项目范围是<code>ItemName</code>或<code>ItemSales</code>中的项目数，以小者为准（标准是1000）。</p><p>购买时，要确定相应的<code>ItemSales</code>是否为非零，或者<code>Money</code>是否大于<code>ItemPrice</code>。</p><p>若购买失败，则会要求再次输入。</p><p>在 Eramaker 中，如果购买失败，用户必须从<code>@Show_Shop</code>重新开始。</p><p>如果购买决策成功，将<code>Item</code>编号分配给<code>Bought</code>变量，将<code>Item:Bought</code>增加1，并通过<code>ItemPrice:Bought</code>减少<code>Money</code>。</p><p>调用<code>@EventBuy</code>，返回<code>@Show_Shop</code>。</p><p>除非调用<code>Begin</code>指令，否则你永远不会离开<code>Shop</code>流程。</p><h2 id="train" tabindex="-1"><a class="header-anchor" href="#train"><span>TRAIN</span></a></h2><p>流程进入方式：</p><ul><li>手动：使用<code>Begin Train</code>语句手动进入。</li></ul><p><img src="https://osdn.net/projects/emuera/wiki/flow/attach/train.gif" alt=""></p><p>进入流程后，首先一些变量会被初始化，如：</p><ul><li><code>AssiPlay:0 = 0</code></li><li><code>PrevCom:0 = -1</code></li><li><code>NextCom:0 = -1</code></li><li><code>TFlag = 0</code></li><li><code>GotJuel</code>、<code>TEquip</code>、<code>EX</code>、<code>Palam</code>和<code>Source</code>中的所有角色都被设置为0。</li><li>最后，为所有角色的<code>STAIN:2</code>分配2，为<code>STAIN:3</code>分配1，为<code>STAIN:4</code>分配8，为其他分配0。</li></ul><p>当你离开<code>Train</code>流程时，这些值不会被初始化，所以如果你在<code>Shop</code>里保存，这些值会保留在你的保存数据中。</p><p>你可以通过<code>@SaveInfo</code>或其他方法将角色的<code>GotJuel</code>、<code>TEquip</code>、<code>EX</code>、<code>Palam</code>等指定为0来保存保存数据的大小。</p><p>这里不解释<code>NextCom</code>的非负值的行为，因为它含有一个严重的 Bug。</p><p>Emuera 的<code>NextCom</code>是为了兼容旧代码而设计的，包括上述的 Bug，且并不打算用于新的用途。</p><p>关于<code>CallTrain</code>指令，见<a href="Command">命令</a>。</p><p>调用<code>@Show_Status</code>后会显示可用的<code>Train</code>。</p><p>会搜索<code>@Com_AbleXX</code>，寻找那些有定义的<code>TrainName</code>。</p><p>搜索范围（图中的<code>Max_Train</code>）对于 Emuera 来说是<code>VariableSize.csv</code>中指定的<code>TrainName</code>范围，对于 Eramaker 来说是<code>2147483647</code>。</p><p>如果<code>@Com_AbleXX</code>没有被定义或返回非零值，那么它就是可用的，<code>TrainName</code>被打印出来。</p><p>如果<code>@Com_AbleXX</code>返回0，则不可用，不显示<code>TrainName</code>。</p><p>在这个时候，它记住了是否可用。 (这并不意味着<code>@Com_AbleXX</code>在运行时被再次调用）。</p><p>显示<code>TrainName</code>后，调用<code>@Show_UserCom</code>。</p><p>在<code>@Show_UserCom</code>之后，输入前初始化<code>Up</code>、<code>Down</code>和<code>LoseBase</code>。</p><p>然后等待用户输入。</p><p>输入的结果与<code>@Com_AbleXX</code>的结果进行核对，如果该命令是可执行的，则调用相应的<code>@ComXX</code>。</p><p>首先，<code>Train</code>号被分配给<code>SelectCom</code>变量，所有字符的<code>NowEX</code>的所有元素被设置为0。</p><p>接下来，<code>@EventCom</code>被调用，接着是相应的<code>@Com</code>。</p><p>如果<code>@Com</code>返回一个非零值，则调用<code>@Source_Check</code>、<code>@EventComEnd</code>并返回<code>@Show_Status</code>。</p><p>在<code>@Source_Check</code>完成后，在调用<code>@EventComEnd</code>前，将所有字符的<code>Source</code>的所有元素设置为0。</p><p>如果在<code>@Source_Check</code>之后，<code>@EventComEnd</code>不存在或者在<code>@EventComEnd</code>中没有给出WAIT指令，那么在<code>@Show_Status</code>之前就会执行一次<code>Wait</code>。</p><p>如果<code>@Com</code>返回0，则返回到<code>@Show_Status</code>。</p><p>注意，当<code>UpCheck</code>指令被执行时，<code>Up</code>和<code>Down</code>的值与<code>Target</code>的<code>Palam</code>相加和相减，并且<code>Up</code>和<code>Down</code>的值都被分配为0。</p><p>如果输入的结果不是可执行的命令，则调用<code>@UserCom</code>并返回<code>@Show_Status</code>。</p><p>除非调用<code>Begin</code>指令，否则你永远不会离开<code>Train</code>流程。</p><h2 id="ablup-升级流程" tabindex="-1"><a class="header-anchor" href="#ablup-升级流程"><span>AblUp 升级流程</span></a></h2><p>流程进入方式：</p><ul><li>手动：使用<code>Begin AblUp</code>语句手动进入。</li></ul><blockquote><p>https://osdn.net/projects/emuera/wiki/flow/attach/ablup.gif</p></blockquote>`,49),n(f,{code:``,preset:`vue`}),s[2]||=i(`<p>调用<code>@Show_Juel</code>和<code>@Show_AblUp_Select</code>后等待用户输入。</p><p>如果输入的范围是0~99，找到相应的<code>@AblUp</code>。</p><p>如果定义了相应的<code>@AblUp</code>，则调用<code>@AblUp</code>并返回到<code>@Show_Juel</code>。</p><p>如果没有定义，就会再次等待用户输入。</p><p>在 Eramaker 中，如果没有定义，就从<code>@Show_Juel</code>重新开始。</p><p>如果输入值在0-99范围之外，则调用<code>@UserAblUp</code>并返回<code>@Show_Juel</code>。</p><p>从 Emuera 1.705开始，没有办法改变这个范围。</p><p>除非调用<code>Begin</code>指令，否则你永远不会离开<code>AblUp</code>流程。</p><h2 id="aftertrain" tabindex="-1"><a class="header-anchor" href="#aftertrain"><span>AfterTrain</span></a></h2><p>流程进入方式：</p><ul><li>手动：使用<code>Begin AfterTrain</code>语句手动进入。</li></ul><p>如果<code>@EventEnd</code>函数在<code>Return</code>之前，没有调用<code>Begin</code>命令，则会报错退出。</p><blockquote><p>https://osdn.net/projects/emuera/wiki/flow/attach/aftertrain.gif</p></blockquote>`,13),n(f,{code:`eJwrLkksKrG1A1NWCo5pJalFIUWJmXlcqWWpeSXxqXkpQMnSpKL80pLMvFQrBQdXkLhrXgoXWApIWCm4FhXlF3GBjdC1g+sDMoGqALlZIsA=`,preset:`vue`}),s[3]||=i(`<h2 id="turnend" tabindex="-1"><a class="header-anchor" href="#turnend"><span>TurnEnd</span></a></h2><p>流程进入方式：</p><ul><li>手动：使用<code>Begin TurnEnd</code>语句手动进入。</li></ul><p>如果<code>@EventTurnEnd</code>函数在<code>Return</code>之前，没有调用<code>Begin</code>命令，则会报错退出。</p><blockquote><p>https://osdn.net/projects/emuera/wiki/flow/attach/turnend.gif</p></blockquote>`,5),n(f,{code:`eJwrLkksKrG1A1NWCiGlRXmueSlcqWWpeSXxJUBefGpeClC6NKkov7QkMy/VSsHBFSQJVwmSBhJWCq5FRflFXGCDdO1QDQDygUoBdv4niQ==`,preset:`vue`}),s[4]||=i(`<h2 id="loadgame" tabindex="-1"><a class="header-anchor" href="#loadgame"><span>LoadGame</span></a></h2><p>流程进入方式：</p><ul><li>手动：使用<code>LoadGame</code>语句手动进入。</li></ul><p><img src="https://osdn.net/projects/emuera/wiki/flow/attach/loadgame.gif" alt=""></p><p><code>Begin</code>指令自带<code>Return</code>特性，<code>Begin</code>指令以下的语句从不执行，但<code>LoadData</code>和<code>SaveData</code>指令与<code>Call</code>指令一样，执行完毕后会返回原处。</p><p>然而，当<code>Load</code>命令被执行时，它就会注销原来的调用位置并进入到<code>LoadDataEnd</code>。</p><h2 id="savegame" tabindex="-1"><a class="header-anchor" href="#savegame"><span>SaveGame</span></a></h2><p>流程进入方式：</p><ul><li>手动：使用<code>SaveGame</code>语句手动进入。</li></ul><p><img src="https://osdn.net/projects/emuera/wiki/flow/attach/savegame.gif" alt=""></p><p>调用<code>@SaveInfo</code>的时机是在实际写完存档之前。</p><h2 id="loaddataend" tabindex="-1"><a class="header-anchor" href="#loaddataend"><span>LoadDataEnd</span></a></h2><p>流程进入方式：</p><ul><li>自动：<code>LoadData</code>命令执行完成后自动进入；</li><li>自动：<code>LoadGame</code>中<code>Load</code>命令执行完成后自动进入；</li></ul><p><img src="https://osdn.net/projects/emuera/wiki/flow/attach/loaddataend1821.gif" alt=""></p><p>当执行<code>Load</code>时，所有以前的状态，包括被调用的函数，都被擦除。</p><p>Eramaker 在这里直接过渡到<code>@SHOW_SHOP</code>。</p><p>在 Emuera 中，如果<code>@System_LoadEnd</code>被定义，则调用。</p><p>如果<code>Begin</code>指令在<code>@System_LoadEnd</code>结束之前被调用，它就会过渡到那里。</p><p>否则，如果定义了<code>@EventLoad</code>，就会执行<code>@EventLoad</code>。</p><p>如果<code>Begin</code>指令在<code>@EventLoad</code>结束前被执行，它就会向该方向移动。</p><p>如果<code>Begin</code>指令没有被执行，则照常过渡到<code>@Show_Shop</code>。</p>`,22)])}var u=a(c,[[`render`,l]]);export{s as _pageData,u as default};