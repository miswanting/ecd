import{X as e,Y as t,ct as n,t as r}from"./chunks/framework.CaFWb3SA.js";var i=JSON.parse(`{"title":"强制配置项目","description":"","frontmatter":{},"headers":[],"relativePath":"translation/Config.md","filePath":"translation/Config.md"}`),a={name:`translation/Config.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="强制配置项目" tabindex="-1">强制配置项目 <a class="header-anchor" href="#强制配置项目" aria-label="Permalink to “强制配置项目”">​</a></h1><blockquote><p>翻译自原文档：<a href="https://osdn.net/projects/emuera/wiki/exconfig" target="_blank" rel="noreferrer">https://osdn.net/projects/emuera/wiki/exconfig</a></p></blockquote><p>如果<code>CSV/</code>文件夹中存在名为<code>_fixed.config</code>和/或<code>_default.config</code>的文件，Emuera将读取这些文件。</p><p>每个.config文件的格式与<code>emuera.config</code>相同。每个项目的含义见配置部分。</p><p>每个文件的优先级取决于Emuera读取配置文件的顺序。</p><p>Emuera读取配置文件的顺序是：</p><ol><li>csv/_default.config</li><li>emuera.config</li><li>csv/_fixed.config</li></ol><p>它将被后来的配置读数所覆盖。也就是说，<code>_default.config</code>中的设置将被<code>emuera.config</code>覆盖，而<code>emuera.config</code>中的设置将被<code>_fixed.config</code>覆盖。注意，这些文件只有在存在上述路径和文件名的情况下才会被加载。</p><p>这意味着，如果你在<code>CSV/</code>文件夹中创建一个子文件夹，并把<code>_fixed.config</code>或<code>_default.config</code>放在它下面，或使用一个没有下划线的文件名，如<code>default.config</code>，它将不会被加载。</p><h2 id="fixed-config" tabindex="-1">_fixed.config <a class="header-anchor" href="#fixed-config" aria-label="Permalink to “_fixed.config”">​</a></h2><p>在<code>_fixed.config</code>中设置的选项优先于<code>emuera.config</code>中的选项。</p><p>另外，<code>_fixed.config</code>中指定的选项不能被Emuera配置对话框所改变。</p><p><code>_fixed.config</code>只有在某些选项对预期行为有要求的情况下才可以使用。</p><p>对于依赖Emuera的换行功能的脚本，必须将<code>不要在按钮中间换行</code>选项设置为<code>YES</code>。</p><p>另外，如果你需要使用<code>_Replace.csv</code>和<code>_Rename.csv</code>，这些选项是必须的。</p><p>如果你使用<code>SETCOLOR</code>，你将需要固定背景和文本的颜色。</p><p>然而，如果你对非必要的选项使用<code>_fixed.config</code>，你将无法对其进行自定义。</p><p>请尽量减少<code>_fixed.config</code>中选项的数量。</p><h2 id="default-config" tabindex="-1">_default.config <a class="header-anchor" href="#default-config" aria-label="Permalink to “_default.config”">​</a></h2><p>如果有些选项不是强制性的，但建议使用，则使用<code>_default.config</code>而不是<code>fixed</code>。</p><p>如果<code>emuera.config</code>不存在，<code>_default.config</code>将作为默认设置。</p><p>如果<code>emuera.config</code>存在，<code>emuera.config</code>中设置的选项将优先于<code>_default.config</code>中设置的选项，并且不会覆盖任何用户设置的选项。</p>`,22)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};