import{_ as e,m as t,p as n,t as r}from"./app-BEQQMb5f.js";var i=JSON.parse(`{"path":"/translation/Config_Settings.html","title":"配置项详解","lang":"zh-CN","frontmatter":{},"git":{"updatedTime":1789617934000,"contributors":[{"name":"Miswanting","username":"Miswanting","email":"ihex@foxmail.com","commits":1,"url":"https://github.com/Miswanting"}],"changelog":[{"hash":"db18c3ae50d9bfd23ae609f018bbbbbbc83aa489","time":1789617934000,"email":"ihex@foxmail.com","author":"Miswanting","message":"feat(reference): add searchable command/error indexes and a version index"}]},"filePathRelative":"translation/Config_Settings.md"}`),a={name:`Config_Settings.md`};function o(r,i,a,o,s,c){return e(),n(`div`,null,[...i[0]||=[t(`<h1 id="配置项详解" tabindex="-1"><a class="header-anchor" href="#配置项详解"><span>配置项详解</span></a></h1><blockquote><p>翻译自原文档：https://osdn.net/projects/emuera/wiki/config</p></blockquote><p>Emuera 首次启动时，会在与 <code>exe</code> 相同的文件夹中创建 <code>emuera.config</code>。</p><p>启动后，从菜单的<code>帮助</code>→<code>设置</code>中也可以修改。此外，直接用文本编辑器改写 <code>emuera.config</code> 也能修改设置，<strong>修改会在下次启动 Emuera 时生效</strong>。用文本编辑器改写时，请注意项目名不要写错。</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="background-color:#222222;color:#E6E6E6;"><pre class="shiki slack-dark vp-code"><code class="language-"><span class="line"><span>○ ボタンの途中で行を折りかえさない:YES</span></span>
<span class="line"><span>× ボタンの途中で行を折りかえさない:ON</span></span>
<span class="line"><span>× ボタンの中途で行を折りかえさない:YES</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>想恢复初始设置时，删除 <code>emuera.config</code> 再启动 Emuera 即可。只有与 <code>exe</code> 同文件夹下的 <code>emuera.config</code> 会起作用。</p><p>如果 <code>csv</code> 文件夹中存在 <code>_fixed.config</code> 或 <code>_default.config</code>，部分项目可能无法修改，或标准值会不同。详情请参阅<a href="Config">强制配置项目</a>。</p><blockquote><p>本页的默认值以 Emuera 源码 <code>Domain/Config/ConfigData.cs</code> 为准。个别项目的文档记载与当前源码不一致时，以源码为准。</p></blockquote><h2 id="环境" tabindex="-1"><a class="header-anchor" href="#环境"><span>环境</span></a></h2><h3 id="マウスを使用する-使用鼠标" tabindex="-1"><a class="header-anchor" href="#マウスを使用する-使用鼠标"><span>マウスを使用する（使用鼠标）</span></a></h3><p>是否接受鼠标输入。</p><h3 id="メニューを使用する-使用菜单" tabindex="-1"><a class="header-anchor" href="#メニューを使用する-使用菜单"><span>メニューを使用する（使用菜单）</span></a></h3><p>是否显示菜单。</p><p>如果设为不显示，将无法从菜单修改设置，只能通过修改 <code>emuera.config</code> 恢复。</p><h3 id="デバッグコマンドを使用する-使用调试命令" tabindex="-1"><a class="header-anchor" href="#デバッグコマンドを使用する-使用调试命令"><span>デバッグコマンドを使用する（使用调试命令）</span></a></h3><p>是否在主控制台接受调试命令。默认为 <code>NO</code>。</p><p>在调试控制台中，无论该选项如何，都可以执行调试命令。</p><h3 id="多重起動を許可する-允许多开" tabindex="-1"><a class="header-anchor" href="#多重起動を許可する-允许多开"><span>多重起動を許可する（允许多开）</span></a></h3><p>是否允许同时启动多个 Emuera。</p><h3 id="キーボードマクロを使用する-使用键盘宏" tabindex="-1"><a class="header-anchor" href="#キーボードマクロを使用する-使用键盘宏"><span>キーボードマクロを使用する（使用键盘宏）</span></a></h3><p>是否使用 <code>F1</code>～<code>F12</code> 的键盘宏。</p><p>另外，如果代码使用了 <code>ONEINPUT</code> 或 <code>ONEINPUTS</code> 指令，键盘宏将无法正常工作，这是规格。</p><h3 id="オートセーブを行なう-进行自动保存" tabindex="-1"><a class="header-anchor" href="#オートセーブを行なう-进行自动保存"><span>オートセーブを行なう（进行自动保存）</span></a></h3><p>是否在执行 <code>BEGIN SHOP</code> 指令的同时进行自动保存。</p><p>该功能可以被 ERB 脚本实质禁用，因此即使开启，也可能实际上没有保存。</p><h3 id="セーブデータをsavフォルダ内に作成する-在-sav-文件夹内创建存档" tabindex="-1"><a class="header-anchor" href="#セーブデータをsavフォルダ内に作成する-在-sav-文件夹内创建存档"><span>セーブデータをsavフォルダ内に作成する（在 sav 文件夹内创建存档）</span></a></h3><p>设置存档放在 <code>exe</code> 同目录下，还是创建 <code>sav</code> 文件夹并放入其中。默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 时如果 <code>sav</code> 文件夹不存在，会弹出对话框询问是否自动移动 <code>sav</code> 文件。回答<code>是</code>后，现有的 <code>global.sav</code> 及 <code>save*.sav</code> 都会被移动到 <code>sav</code> 文件夹内。从 <code>YES</code> 改回 <code>NO</code> 时，请手动移动 <code>sav</code> 文件。</p><h3 id="履歴ログの行数-历史日志行数" tabindex="-1"><a class="header-anchor" href="#履歴ログの行数-历史日志行数"><span>履歴ログの行数（历史日志行数）</span></a></h3><p>保存历史日志的最大行数。下限为 500 行。</p><h3 id="無限ループ警告までのミリ秒数-无限循环警告的毫秒数" tabindex="-1"><a class="header-anchor" href="#無限ループ警告までのミリ秒数-无限循环警告的毫秒数"><span>無限ループ警告までのミリ秒数（无限循环警告的毫秒数）</span></a></h3><p>在指定时间内没有执行 <code>WAIT</code> 系指令时，弹出警告对话框。</p><p>设为 <code>0</code> 时该功能无效。</p><p>下面这样的脚本虽然是无限循环，但中间夹了 <code>WAIT</code>，因此不会被警告：</p><div class="language-erb line-numbers-mode" data-highlighter="shiki" data-ext="erb" style="background-color:#222222;color:#E6E6E6;"><pre class="shiki slack-dark vp-code"><code class="language-erb"><span class="line"><span style="color:#E6E6E6;">$LOOP</span></span>
<span class="line"><span style="color:#569CD6;">WAIT</span></span>
<span class="line"><span style="color:#C586C0;">GOTO</span><span style="color:#9CDCFE;"> LOOP</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>用右键或 Esc 跳过消息时，只要中间夹了 <code>WAIT</code> 也不会被警告。</p><h3 id="使用するセーブデータ数-使用的存档数量" tabindex="-1"><a class="header-anchor" href="#使用するセーブデータ数-使用的存档数量"><span>使用するセーブデータ数（使用的存档数量）</span></a></h3><p>存档/读取画面显示的存档数量。下限为 20（与 Eramaker 相同），上限为 80。</p><h3 id="関連づけるテキストエディタ-关联的文本编辑器" tabindex="-1"><a class="header-anchor" href="#関連づけるテキストエディタ-关联的文本编辑器"><span>関連づけるテキストエディタ（关联的文本编辑器）</span></a></h3><p>点击错误信息时，用这里设置的文本编辑器打开对应文件。</p><h3 id="コマンドライン引数-命令行参数" tabindex="-1"><a class="header-anchor" href="#コマンドライン引数-命令行参数"><span>コマンドライン引数（命令行参数）</span></a></h3><p>把文件传给文本编辑器时附加的命令行参数。</p><h2 id="显示" tabindex="-1"><a class="header-anchor" href="#显示"><span>显示</span></a></h2><h3 id="描画インターフェース-绘制接口" tabindex="-1"><a class="header-anchor" href="#描画インターフェース-绘制接口"><span>描画インターフェース（绘制接口）</span></a></h3><p>用于绘制的接口。可选值：</p><ul><li><code>WINAPI</code>：用 <code>gdi32.dll</code> 的 <code>TextOut</code> 函数绘制（GDI 绘制）。1.700 追加的绘制方式，比其它方式更快（但在某些环境下反而更慢）。</li><li><code>GRAPHICS</code>：用 <code>Graphics.DrawString</code> 函数绘制（GDI+ 绘制）。等同于旧选项<code>使用 GDI+:YES</code>。GDI+ 的特点是非等宽字体也可能出现字符串宽度偏移。</li><li><code>TEXTRENDERER</code>：用 <code>TextRenderer.DrawText</code> 函数绘制（GDI 绘制）。等同于旧选项<code>使用 GDI+:NO</code>。</li></ul><blockquote><p>源码中的默认值为 <code>TEXTRENDERER</code>。</p></blockquote><h3 id="フレーム毎秒-每秒帧数" tabindex="-1"><a class="header-anchor" href="#フレーム毎秒-每秒帧数"><span>フレーム毎秒（每秒帧数）</span></a></h3><p>一秒最多绘制多少次。设小一些可以让运行更快。</p><p>如果<code>最大スキップフレーム数</code>较小，那么即使把每秒帧数设小也不会变快。</p><h3 id="printcを並べる数-并排的-printc-数量" tabindex="-1"><a class="header-anchor" href="#printcを並べる数-并排的-printc-数量"><span>PRINTCを並べる数（并排的 PRINTC 数量）</span></a></h3><p><code>PRINT_SHOPITEM</code> 等系统执行 <code>PRINTC</code> 指令时，每几个换一次行。Eramaker 原作中为 3。</p><p>设为 <code>0</code> 时，不按 <code>PRINTC</code> 数量折行。此时请同时把<code>ボタンの途中で行を折りかえさない</code>设为 <code>YES</code>。这样当内容超出窗口尺寸时会自动折行。</p><h3 id="printcの文字数-printc-的字符数" tabindex="-1"><a class="header-anchor" href="#printcの文字数-printc-的字符数"><span>PRINTCの文字数（PRINTC 的字符数）</span></a></h3><p><code>PRINTC</code> 一共补足到多少个字符（在左侧补半角空格）。Eramaker 原作中为 25。</p><p>显示商店商品时使用该字符数 +1。Eramaker 中商店商品会格式化为 26 个字符。</p><p>一行能排下的字符数大约为 <code>(窗口宽度) / (字体大小) * 2</code>，请让 <code>(PRINTC 的字符数+1) * (并排的 PRINTC 数量)</code> 小于它。</p><h3 id="ボタンの途中で行を折りかえさない-不在按钮中间折行" tabindex="-1"><a class="header-anchor" href="#ボタンの途中で行を折りかえさない-不在按钮中间折行"><span>ボタンの途中で行を折りかえさない（不在按钮中间折行）</span></a></h3><p>所谓按钮，是指 <code>[200] - セーブ</code> 这样可用鼠标选择、点击的字符串。</p><p>初始值为 <code>NO</code>，与 Eramaker 一样显示。设为 <code>YES</code> 时，会把超出窗口的整个按钮移到下一行。</p><h2 id="窗口" tabindex="-1"><a class="header-anchor" href="#窗口"><span>窗口</span></a></h2><h3 id="ウィンドウ幅-窗口宽度" tabindex="-1"><a class="header-anchor" href="#ウィンドウ幅-窗口宽度"><span>ウィンドウ幅（窗口宽度）</span></a></h3><p>启动时的窗口宽度。是绘制区域的宽度，不是整体尺寸。</p><h3 id="ウィンドウ高さ-窗口高度" tabindex="-1"><a class="header-anchor" href="#ウィンドウ高さ-窗口高度"><span>ウィンドウ高さ（窗口高度）</span></a></h3><p>启动时的窗口高度。是绘制区域的高度，但包含最下方输入部分的高度。</p><h3 id="ウィンドウの高さを可変にする-窗口高度可调整" tabindex="-1"><a class="header-anchor" href="#ウィンドウの高さを可変にする-窗口高度可调整"><span>ウィンドウの高さを可変にする（窗口高度可调整）</span></a></h3><p>运行时是否可以改变窗口大小。</p><h3 id="起動時にウィンドウを最大化する-启动时最大化窗口" tabindex="-1"><a class="header-anchor" href="#起動時にウィンドウを最大化する-启动时最大化窗口"><span>起動時にウィンドウを最大化する（启动时最大化窗口）</span></a></h3><p>默认 <code>NO</code>。设为 <code>YES</code> 时，Emuera 会以最大化状态启动。</p><h3 id="起動時のウィンドウの位置を固定する-固定启动时的窗口位置" tabindex="-1"><a class="header-anchor" href="#起動時のウィンドウの位置を固定する-固定启动时的窗口位置"><span>起動時のウィンドウの位置を固定する（固定启动时的窗口位置）</span></a></h3><p>运行时是否使用指定的窗口位置。不指定时，<code>ウィンドウ位置X</code>、<code>Y</code> 没有意义。</p><h3 id="ウィンドウ位置x-ウィンドウ位置y" tabindex="-1"><a class="header-anchor" href="#ウィンドウ位置x-ウィンドウ位置y"><span>ウィンドウ位置X / ウィンドウ位置Y</span></a></h3><p>固定窗口位置时的坐标。显示器左上角为 <code>(0,0)</code>，X 越大越靠右，Y 越大越靠下。</p><h3 id="スクロールの行数-滚动行数" tabindex="-1"><a class="header-anchor" href="#スクロールの行数-滚动行数"><span>スクロールの行数（滚动行数）</span></a></h3><p>鼠标滚轮滚动一次移动的行数。</p><h2 id="字体" tabindex="-1"><a class="header-anchor" href="#字体"><span>字体</span></a></h2><h3 id="背景色" tabindex="-1"><a class="header-anchor" href="#背景色"><span>背景色</span></a></h3><p>背景颜色。</p><h3 id="文字色" tabindex="-1"><a class="header-anchor" href="#文字色"><span>文字色</span></a></h3><p>文字颜色。脚本中执行了 <code>SETCOLOR</code> 指令时以该指令优先。</p><h3 id="選択中文字色" tabindex="-1"><a class="header-anchor" href="#選択中文字色"><span>選択中文字色</span></a></h3><p>鼠标选中按钮时的文字颜色。</p><h3 id="履歴文字色" tabindex="-1"><a class="header-anchor" href="#履歴文字色"><span>履歴文字色</span></a></h3><p>显示历史记录（backlog）时的文字颜色。脚本中执行了 <code>SETCOLOR</code> 指令时以该指令优先。</p><h3 id="フォント名-字体名" tabindex="-1"><a class="header-anchor" href="#フォント名-字体名"><span>フォント名（字体名）</span></a></h3><p>指定字体。不能指定点阵字体。</p><p>也可以指定比例字体，但会导致显示错乱，不推荐。</p><h3 id="フォントサイズ-字号" tabindex="-1"><a class="header-anchor" href="#フォントサイズ-字号"><span>フォントサイズ（字号）</span></a></h3><p>以像素为单位指定文字的大小（高度）。</p><h3 id="一行の高さ-行高" tabindex="-1"><a class="header-anchor" href="#一行の高さ-行高"><span>一行の高さ（行高）</span></a></h3><p>以像素为单位指定一行的高度。不能小于字号。</p><p>行高与字号相同时可能出现显示错乱，建议设为字号 +1～2。推荐 <code>字号 = 18</code>、<code>行高 = 19</code>。</p><h2 id="系统" tabindex="-1"><a class="header-anchor" href="#系统"><span>系统</span></a></h2><p>这些是设想由 ERB 作者决定的选项。修改<code>系统</code>的项目后，ERB 脚本可能无法按作者意图工作。</p><h3 id="大文字小文字の違いを無視する-忽略大小写差异" tabindex="-1"><a class="header-anchor" href="#大文字小文字の違いを無視する-忽略大小写差异"><span>大文字小文字の違いを無視する（忽略大小写差异）</span></a></h3><p>是否忽略 ERB 中命令和变量的大小写差异。默认为 <code>YES</code>（忽略）。</p><h3 id="rename-csvを利用する-使用-rename-csv" tabindex="-1"><a class="header-anchor" href="#rename-csvを利用する-使用-rename-csv"><span>_Rename.csvを利用する（使用 _Rename.csv）</span></a></h3><p>是否使用 EraMakerEx 的 <code>_Rename.csv</code>。默认为 <code>NO</code>（不使用）。</p><p><code>_Rename.csv</code> 的规格请参阅 EraMakerEx 的 <code>Readme.txt</code>（Emuera 未附带）。</p><h3 id="replace-csvを利用する-使用-replace-csv" tabindex="-1"><a class="header-anchor" href="#replace-csvを利用する-使用-replace-csv"><span>_Replace.csvを利用する（使用 _Replace.csv）</span></a></h3><p>是否使用私家改造版 1.52a-c 追加的 <code>_Replace.csv</code> 功能。</p><h3 id="サブディレクトリを検索する-搜索子目录" tabindex="-1"><a class="header-anchor" href="#サブディレクトリを検索する-搜索子目录"><span>サブディレクトリを検索する（搜索子目录）</span></a></h3><p>是否读取 <code>CSV</code> 与 <code>ERB</code> 文件夹中子文件夹里的文件。默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 时，<code>chara*.csv</code> 和 <code>*.ERB</code> 也会把子文件夹内的文件作为读取对象。<code>chara*.csv</code> 以外的 csv 文件不在此选项范围内。</p><p>读取顺序为：文件夹内的文件和文件夹在前，其它文件在后。启用<code>読み込み順をファイル名順にソートする</code>时，文件夹也会按名称排序。</p><h3 id="読み込み順をファイル名順にソートする-按文件名排序读取顺序" tabindex="-1"><a class="header-anchor" href="#読み込み順をファイル名順にソートする-按文件名排序读取顺序"><span>読み込み順をファイル名順にソートする（按文件名排序读取顺序）</span></a></h3><p>是否把文件读取顺序按文件名排序。默认为 <code>NO</code>。</p><p>关于文件系统与读取顺序相关的问题，请参阅<a href="Difference">与 Eramaker 的差异</a>中的对应条目。</p><ul><li><code>NO</code> 时，读取顺序保持 C# <code>Directory.GetFiles</code> 函数返回的顺序。该函数内部使用 <code>Kernel32.dll</code> 的 <code>FindNextFile</code>，结果取决于文件系统。它大概能重现 Eramaker 的读取顺序，但也会导致脚本行为随用户环境而不同。</li><li><code>YES</code> 时，读取前会先按文件名排序。注意排序结果与 NTFS 的文件顺序并不一致：例如 <code>_</code>（下划线）在 NTFS 中排在数字和字母之前，而按文件名排序时排在数字之后。</li></ul><h3 id="システム関数の上書きを許可する-允许覆盖系统函数" tabindex="-1"><a class="header-anchor" href="#システム関数の上書きを許可する-允许覆盖系统函数"><span>システム関数の上書きを許可する（允许覆盖系统函数）</span></a></h3><p>是否允许用户定义函数覆盖<a href="Expression">表达式内函数</a>。</p><p>默认 <code>YES</code>（允许）。</p><h3 id="システム関数が上書きされたとき警告を表示する-系统函数被覆盖时显示警告" tabindex="-1"><a class="header-anchor" href="#システム関数が上書きされたとき警告を表示する-系统函数被覆盖时显示警告"><span>システム関数が上書きされたとき警告を表示する（系统函数被覆盖时显示警告）</span></a></h3><p>是否在用户定义函数覆盖了<a href="Expression">表达式内函数</a>时显示警告。默认 <code>YES</code>（显示）。</p><p>当<code>システム関数の上書きを許可する</code>为 <code>YES</code> 时，无论本项设置如何都会显示警告。</p><h3 id="同名の非イベント関数が複数定義されたとき警告する-同名非事件函数重复定义时警告" tabindex="-1"><a class="header-anchor" href="#同名の非イベント関数が複数定義されたとき警告する-同名非事件函数重复定义时警告"><span>同名の非イベント関数が複数定義されたとき警告する（同名非事件函数重复定义时警告）</span></a></h3><p>在 Eramaker 的系统中，即使有多个同名函数，也只有最先读取的生效，之后会被忽略。</p><p>这一规格有时有用，但也会导致脚本行为随用户环境而不同。把本项设为 <code>YES</code>（警告）后，同名函数存在多个时会给出警告。</p><h3 id="全角スペースをホワイトスペースに含める-把全角空格也算作空白" tabindex="-1"><a class="header-anchor" href="#全角スペースをホワイトスペースに含める-把全角空格也算作空白"><span>全角スペースをホワイトスペースに含める（把全角空格也算作空白）</span></a></h3><p>默认为 <code>YES</code>（包含）。设为 <code>NO</code> 后，就不能把全角空格当作空白使用，也就是说，可以禁止在 ERB 中用全角空格代替半角空格或制表符来缩进。</p><h3 id="内部で使用する東アジア言語-内部使用的东亚语言" tabindex="-1"><a class="header-anchor" href="#内部で使用する東アジア言語-内部使用的东亚语言"><span>内部で使用する東アジア言語（内部使用的东亚语言）</span></a></h3><p>对于 <code>STRLENS</code> 等依赖内部区域设置的处理，可以选择使用的区域设置。UI 等不会改变。默认为日语（<code>ja-JP</code>）。</p><p>支持以下语言：</p><ul><li>日语（Shift-JIS，默认）</li><li>韩语（<code>ko-KR</code>）</li><li>简体中文（<code>zh-CN</code>，未测试）</li><li>繁体中文（<code>zh-TW</code>，未测试）</li></ul><h3 id="form中の三連記号を展開しない-不展开-form-中的三连符号" tabindex="-1"><a class="header-anchor" href="#form中の三連記号を展開しない-不展开-form-中的三连符号"><span>FORM中の三連記号を展開しない（不展开 FORM 中的三连符号）</span></a></h3><p>Eramaker 的 FORM 语法中，<code>///</code>、<code>+++</code> 等三连符号会展开为 <code>NAME:ASSI</code>、<code>CALLNAME:ASSI</code> 等字符串。</p><p>把本项设为 <code>YES</code>（不展开）后，会保留符号本身。默认为 <code>NO</code>（展开）。</p><h3 id="セーブデータをバイナリ形式で保存する-以二进制格式保存存档" tabindex="-1"><a class="header-anchor" href="#セーブデータをバイナリ形式で保存する-以二进制格式保存存档"><span>セーブデータをバイナリ形式で保存する（以二进制格式保存存档）</span></a></h3><p>是否以二进制形式记录存档。默认为 <code>NO</code>。</p><p><code>NO</code> 时，存档的文字编码与 Eramaker 语法相同。二进制形式不一定更小，反而可能更大，修改时请注意。</p><p>使用 <code>#DIM SAVEDATA</code> 定义可保存的角色型变量、多维字符串变量时，<strong>必须</strong>把本项设为 <code>YES</code>。</p><h3 id="セーブデータをutf-8で保存する-以-utf-8-保存存档" tabindex="-1"><a class="header-anchor" href="#セーブデータをutf-8で保存する-以-utf-8-保存存档"><span>セーブデータをUTF-8で保存する（以 UTF-8 保存存档）</span></a></h3><p>是否以 UTF-8 记录存档。默认为 <code>NO</code>。</p><p><code>NO</code> 时，存档编码与 Eramaker 一样是 SJIS。当<code>セーブデータをバイナリ形式で保存する</code>为 <code>YES</code> 时，本项无效（强制使用 UTF-8）。</p><h3 id="oneinput系命令でマウスによる2文字以上の入力を許可する-允许-oneinput-系用鼠标输入多个字符" tabindex="-1"><a class="header-anchor" href="#oneinput系命令でマウスによる2文字以上の入力を許可する-允许-oneinput-系用鼠标输入多个字符"><span>ONEINPUT系命令でマウスによる2文字以上の入力を許可する（允许 ONEINPUT 系用鼠标输入多个字符）</span></a></h3><p>该选项不显示在设置画面中，只能直接编辑 <code>emuera.config</code> 来设置。默认 <code>NO</code>（不许可）。</p><p>为 <code>YES</code> 时，<code>ONEINPUT</code> 系在鼠标一次输入两个及以上字符时，不再截断为只取首字符。</p><h2 id="兼容性" tabindex="-1"><a class="header-anchor" href="#兼容性"><span>兼容性</span></a></h2><p>这些选项用于解决“Eramaker 与 Emuera 行为不同”“在 Emuera 旧版本能运行的脚本升级后不能运行”等问题。标准设置下没问题时请不要修改。</p><h3 id="解釈不能な行があっても実行する-即使有无法解释的行也继续执行" tabindex="-1"><a class="header-anchor" href="#解釈不能な行があっても実行する-即使有无法解释的行也继续执行"><span>解釈不能な行があっても実行する（即使有无法解释的行也继续执行）</span></a></h3><p>默认为 <code>NO</code>，即启动时若存在解释失败的行，会在标题画面报错终止。</p><p>本项用于停止该行为，让存在无法解释的行时也能从标题继续。</p><h3 id="callnameが空文字列の時にnameを代入する-callname-为空字符串时用-name-代替" tabindex="-1"><a class="header-anchor" href="#callnameが空文字列の時にnameを代入する-callname-为空字符串时用-name-代替"><span>CALLNAMEが空文字列の時にNAMEを代入する（CALLNAME 为空字符串时用 NAME 代替）</span></a></h3><p>默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 时，如果 <code>chara*.csv</code> 中没有设置 <code>CALLNAME</code>，或设置成了空字符串，就当作设置了与 <code>NAME</code> 相同的字符串。这是为了重现 Eramaker 中 <code>CALLNAME</code> 为空时返回 <code>NAME</code> 的行为。</p><p>但即使使用本项也无法完全重现。例如用 Eramaker 添加角色的存档在 Emuera 中读取时，行为可能不同。</p><h3 id="擬似変数randの仕様をeramakerに合わせる-让-rand-的规格与-eramaker-一致" tabindex="-1"><a class="header-anchor" href="#擬似変数randの仕様をeramakerに合わせる-让-rand-的规格与-eramaker-一致"><span>擬似変数RANDの仕様をeramakerに合わせる（让 RAND 的规格与 Eramaker 一致）</span></a></h3><p>默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 时，<code>RAND</code> 的生成方式与 Eramaker 相同。该方式的特点是：参数为负值也能工作、不会返回 32767 以上的值、<code>X</code> 超过 1000 左右时偏差不可忽视等。详情请参阅<a href="Difference">与 Eramaker 的差异</a>。</p><h3 id="関数・属性については大文字小文字を無視しない-函数、属性区分大小写" tabindex="-1"><a class="header-anchor" href="#関数・属性については大文字小文字を無視しない-函数、属性区分大小写"><span>関数・属性については大文字小文字を無視しない（函数、属性区分大小写）</span></a></h3><p>默认为 <code>NO</code>。</p><p>Eramaker 对命令和变量不区分大小写，但对函数名、属性区分大小写。把本项设为 <code>YES</code>，并同时把<code>大文字小文字の違いを無視する</code>设为 <code>YES</code>，即可实现与 Eramaker 相同的行为。</p><h3 id="イベント関数のcallを許可する-允许-call-事件函数" tabindex="-1"><a class="header-anchor" href="#イベント関数のcallを許可する-允许-call-事件函数"><span>イベント関数のCALLを許可する（允许 CALL 事件函数）</span></a></h3><p>默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 后，可以用 <code>CALL</code> 指令调用事件函数，规格与 Eramaker 相同。</p><h3 id="ver1739以前の非ボタン折り返しを再現する-重现-ver1739-以前的非按钮折行" tabindex="-1"><a class="header-anchor" href="#ver1739以前の非ボタン折り返しを再現する-重现-ver1739-以前的非按钮折行"><span>ver1739以前の非ボタン折り返しを再現する（重现 ver1739 以前的非按钮折行）</span></a></h3><p>默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 后，会重现 Emuera 1.739 以前的 <code>DRAWLINE</code> 及其它规格。这样依赖 ver1739 以前非按钮折行规格的显示脚本等就能正常工作。升级 Emuera 后显示错乱时，把本项设为 <code>YES</code> 可能解决问题。</p><h3 id="ユーザー関数の全ての引数の省略を許可する-允许省略用户函数的全部参数" tabindex="-1"><a class="header-anchor" href="#ユーザー関数の全ての引数の省略を許可する-允许省略用户函数的全部参数"><span>ユーザー関数の全ての引数の省略を許可する（允许省略用户函数的全部参数）</span></a></h3><p>默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 后，即使把 <code>ARG</code>、<code>ARGS</code>、用户定义私有变量以外的变量设作函数参数，调用时也可以忽略参数。这样可以重现 Emuera 1.807 以前“省略这些变量作参数时不赋值、保持调用前状态”的规格。从 Emuera 1.807 以前升级后函数报错时，把本项设为 <code>YES</code> 可能解决问题。</p><h3 id="ユーザー関数の引数に自動的にtostrを補完する-自动为用户函数参数补上-tostr" tabindex="-1"><a class="header-anchor" href="#ユーザー関数の引数に自動的にtostrを補完する-自动为用户函数参数补上-tostr"><span>ユーザー関数の引数に自動的にTOSTRを補完する（自动为用户函数参数补上 TOSTR）</span></a></h3><p>默认为 <code>NO</code>。</p><p>设为 <code>YES</code> 后，与 Emuera 1.807 以前一样，调用函数时给字符串型参数传数值会自动转换为字符串型。从 Emuera 1.807 以前升级后函数报错时，把本项设为 <code>YES</code> 可能解决问题。</p><blockquote><p>在 Emuera 1.820（Emuera1819+v10）之前的一段时间里，该选项的效果曾经是相反的。</p></blockquote><h3 id="spキャラを使用する-使用-sp-角色" tabindex="-1"><a class="header-anchor" href="#spキャラを使用する-使用-sp-角色"><span>SPキャラを使用する（使用 SP 角色）</span></a></h3><p>默认为 <code>NO</code>。</p><p>本项用于恢复 1.816 中删除的 SP 角色相关功能。从 Emuera 1.816 以前升级后报错时，把本项设为 <code>YES</code> 可能解决问题。</p><h2 id="解析" tabindex="-1"><a class="header-anchor" href="#解析"><span>解析</span></a></h2><h3 id="eramaker互換性に関する警告を表示する-显示-eramaker-兼容性警告" tabindex="-1"><a class="header-anchor" href="#eramaker互換性に関する警告を表示する-显示-eramaker-兼容性警告"><span>eramaker互換性に関する警告を表示する（显示 Eramaker 兼容性警告）</span></a></h3><p>显示与 Eramaker 兼容性相关的警告。</p><h3 id="ロード時にレポートを表示する-加载时显示报告" tabindex="-1"><a class="header-anchor" href="#ロード時にレポートを表示する-加载时显示报告"><span>ロード時にレポートを表示する（加载时显示报告）</span></a></h3><p>是否在读取时显示总行数、函数数量等报告。</p><p>不显示时，会显示 <code>_replace.csv</code> 中指定的加载中消息。</p><h3 id="ロード時に引数を解析する-加载时解析参数" tabindex="-1"><a class="header-anchor" href="#ロード時に引数を解析する-加载时解析参数"><span>ロード時に引数を解析する（加载时解析参数）</span></a></h3><p>是否在读取时解析到指令的参数。读取时解析会让加载明显变慢，但执行略微变快。如果目的不是错误检查，设为 <code>NO</code> 会更舒适。</p><ul><li><code>NO</code>：不解析。</li><li><code>ONCE</code>：仅当上次启动以来 csv 或 ERB 有更新时解析。</li><li><code>YES</code>：总是解析。</li></ul><h3 id="表示する最低警告レベル-显示的最低警告等级" tabindex="-1"><a class="header-anchor" href="#表示する最低警告レベル-显示的最低警告等级"><span>表示する最低警告レベル（显示的最低警告等级）</span></a></h3><p>读取时显示的警告等级。低于指定值的警告不显示。</p><ul><li><code>0</code>：如用 <code>==</code> 代替 <code>=</code> 之类不会造成特别问题的警告。</li><li><code>1</code>：如在函数声明以外使用 <code>#PRI</code> 之类可以忽略执行的问题。</li><li><code>2</code>：无法解释的行、没有对应 <code>IF</code>～<code>ENDIF</code> 等运行时会出错的行。</li><li><code>3</code>：读取中发生的无法恢复的错误，会被强制显示。</li></ul><h3 id="呼び出されなかった関数を無視する-忽略未被调用的函数" tabindex="-1"><a class="header-anchor" href="#呼び出されなかった関数を無視する-忽略未被调用的函数"><span>呼び出されなかった関数を無視する（忽略未被调用的函数）</span></a></h3><p>仅在<code>ロード時に引数を解析する</code>时有效。</p><p>设为 <code>YES</code> 后，对被调用的函数不进行参数解析。</p><h3 id="関数が見つからない警告の扱い-找不到函数警告的处理" tabindex="-1"><a class="header-anchor" href="#関数が見つからない警告の扱い-找不到函数警告的处理"><span>関数が見つからない警告の扱い（找不到函数警告的处理）</span></a></h3><p>仅在<code>ロード時に引数を解析する</code>时有效。设置 <code>CALL</code>、<code>JUMP</code> 的目标函数不存在时是否警告。</p><ul><li><code>IGNORE</code>：不警告。</li><li><code>LATER</code>：不警告，但之后只报告被忽略的数量。</li><li><code>ONCE</code>：每个文件只警告一次。</li><li><code>DISPLAY</code>：全部显示。注意 <code>CALLFORM</code>、<code>JUMPFORM</code> 等不在本项范围内。</li></ul><h3 id="関数が呼び出されなかった警告の扱い-函数未被调用警告的处理" tabindex="-1"><a class="header-anchor" href="#関数が呼び出されなかった警告の扱い-函数未被调用警告的处理"><span>関数が呼び出されなかった警告の扱い（函数未被调用警告的处理）</span></a></h3><p>仅在<code>ロード時に引数を解析する</code>时有效。设置 <code>@～～</code> 已定义却一次也没被调用时是否警告。格式与<code>関数が見つからない警告の扱い</code>相同。</p><h2 id="调试" tabindex="-1"><a class="header-anchor" href="#调试"><span>调试</span></a></h2><p>调试相关的选项只在以调试模式启动 Emuera 时有效。详情请参阅<a href="Debug_Mode">调试模式</a>。</p><h3 id="起動時にデバッグウインドウを表示する-启动时显示调试窗口" tabindex="-1"><a class="header-anchor" href="#起動時にデバッグウインドウを表示する-启动时显示调试窗口"><span>起動時にデバッグウインドウを表示する（启动时显示调试窗口）</span></a></h3><p>以调试模式启动时自动显示调试窗口。</p><h3 id="デバッグウインドウを最前面に表示する-调试窗口置顶显示" tabindex="-1"><a class="header-anchor" href="#デバッグウインドウを最前面に表示する-调试窗口置顶显示"><span>デバッグウインドウを最前面に表示する（调试窗口置顶显示）</span></a></h3><p>打开调试窗口时，默认置顶显示。打开后可以用调试窗口内的按钮切换。</p><h3 id="デバッグウインドウ幅-デバッグウインドウ高さ-调试窗口宽-高" tabindex="-1"><a class="header-anchor" href="#デバッグウインドウ幅-デバッグウインドウ高さ-调试窗口宽-高"><span>デバッグウインドウ幅 / デバッグウインドウ高さ（调试窗口宽 / 高）</span></a></h3><p>调试窗口的宽度 / 高度，包含窗口边框的整体尺寸。</p><p>与主窗口不同，调试窗口打开后仍可自由改变大小。要使用设置对话框的<code>获取当前窗口大小</code>按钮，需要先打开调试窗口，再打开设置对话框。</p><h3 id="デバッグウインドウ位置を指定する-指定调试窗口位置" tabindex="-1"><a class="header-anchor" href="#デバッグウインドウ位置を指定する-指定调试窗口位置"><span>デバッグウインドウ位置を指定する（指定调试窗口位置）</span></a></h3><p>是否指定打开调试窗口时的位置。不指定时，<code>デバッグウィンドウ位置X</code>、<code>Y</code> 没有意义。</p><h3 id="デバッグウィンドウ位置x-y" tabindex="-1"><a class="header-anchor" href="#デバッグウィンドウ位置x-y"><span>デバッグウィンドウ位置X / Y</span></a></h3><p>固定调试窗口位置时的坐标。显示器左上角为 <code>(0,0)</code>，X 越大越靠右，Y 越大越靠下。</p>`,201)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};